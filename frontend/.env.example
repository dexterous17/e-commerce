# Vite dev server proxies /api and /uploads here. Must match backend/.env PORT when you
# run `npm start` in backend/ (default 5002; see backend/.env.example).
# Not VITE_* on purpose: only VITE_-prefixed keys belong in client bundles (import.meta.env).
DEV_PROXY_TARGET=http://localhost:5002
