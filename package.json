{
  "name": "e-commerce",
  "private": true,
  "scripts": {
    "start": "npm run dev --prefix backend",
    "dev": "npm run dev --prefix backend",
    "dev:all": "npm run dev --prefix frontend",
    "start:frontend": "npm start --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start:api": "npm start --prefix backend",
    "build": "node scripts/build-for-live.mjs",
    "build:live": "node scripts/build-for-live.mjs",
    "build:live:strict": "node scripts/build-for-live.mjs --strict",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:all": "npm run build",
    "postgres:up": "docker compose up -d postgres",
    "minio:up": "docker compose up -d minio minio-init",
    "minio:mirror-to-s3": "bash scripts/sync-minio-to-s3.sh",
    "publish-demo-videos": "bash scripts/publish_demo_videos_release.sh",
    "db:validate-schema": "npm run db:validate-schema --prefix backend",
    "db:validate-schema:strict": "npm run db:validate-schema:strict --prefix backend"
  }
}
