# Copy to backend/db/.env.production for merged deploy bundles (scripts/build-for-live.mjs).
# Docker: path inside the container (see docker-compose.yml / docker-compose.lightsail.yml).

SQLITE_DATABASE_PATH=/app/backend/data/ecommerce.sqlite
