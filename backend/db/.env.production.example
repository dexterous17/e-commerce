# Copy to backend/db/.env.production for merged deploy bundles (scripts/build-for-live.mjs).

# Example: managed Postgres (use the URL your provider gives you)
DATABASE_URL=postgresql://USER:PASSWORD@your-db.region.provider.com:5432/ecommerce?sslmode=require
