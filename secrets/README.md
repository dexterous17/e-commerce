Create or update these local files for Docker Compose secrets:

- `secrets/jwt_secret.txt`

These files are git-ignored.

The backend also supports additional `*_FILE` environment variables if you want to mount more Docker secrets later.
