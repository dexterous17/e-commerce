# Legacy path: still loaded if present (after backend/db/.env). For new setups prefer:
#   cp backend/db/.env.example backend/db/.env
#
# DATABASE_URL=postgresql://USER:PASSWORD@127.0.0.1:5432/ecommerce
